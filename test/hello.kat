func add(x: int, y: int) => int {
  return x + y;
}

func main() {
  char c = 'c';
  str s = "hello, kat";
  print(s);

  int res = add(1, 2);
  if (res == 3) {
    meow("YES");
  } else {
    meow("NO");
  }

  int i = 0, sum = 0;
  while (i <= 10) {
    sum = sum + i;
    i = i + 1;
  }

  return 0;
}