func add(a: int, b: int) => int {
  return a + b;
}

func main(argc: int, argv: str) => int {
  let x: int = (1 + 2) * 3;
  let y: int = 4 * (5 - 6);
  let res: int = add(x, y) + 10;

	let z: bool = (x == 9);
	let c: char;
  if (z) {
    c = 'Y';
  } else {
    c = 'N';
  }

  let i: int = 0;
  let sum: int = 0;
  while (i <= 10) {
    sum = sum + i;
  }

  return 0;
}